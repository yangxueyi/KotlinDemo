<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zhang.kotlindemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.KotlinGaoJieActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity"/>
        <activity android:name=".activity.KotlinActivity"/>
        <activity android:name=".activity.LambdaAndListActivity"/>
        <activity android:name=".activity.LambdaAndSAMActivity"/>
        <activity android:name=".activity.LambdaNullTypeActivity"/>
        <activity android:name=".activity.LambdaBasicTypeActivity"/>
        <activity android:name=".activity.KotlinChongZaiActivity"/>
    </application>

</manifest>